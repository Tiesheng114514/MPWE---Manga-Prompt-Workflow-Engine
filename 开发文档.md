# MPWE 开发文档

> 本文档面向**开发者**（架构、接口、扩展方式）。普通用户 / 部署者请看根目录的 [README.md](README.md)。

漫画创作工作流引擎：前端 WebUI（选项式操作）+ 后端 Python（调用本地 ComfyUI Server API 完成绘图）。

## 架构分层（前后端分离）

```text
前端：WebUI（app/webui/，浏览器端，localhost:8642）
  ↓ HTTP /api
后端：我们的 API 服务（app/，FastAPI）
  ↓ ComfyUI Server API
后端：ComfyUI 绘图引擎（http://127.0.0.1:8188）
```

## 目录结构

```text
MPWE---Manga-Prompt-Workflow-Engine/
├── .venv/                  # Python 3.14.6 虚拟环境
├── app/                    # 应用代码（前端 + 后端，按层/按功能组织）
│   ├── __init__.py
│   ├── main.py             # 入口：启动 FastAPI 并托管 WebUI
│   ├── admin_server.py     # 管理员后台独立服务（端口 8643，与用户服务分离）
│   ├── config.py           # 配置加载（config.yaml + 环境变量）
│   ├── api/                # 接口层：REST 路由（WebUI 唯一通信入口）
│   │   └── routes.py
│   ├── core/               # 核心组件（不依赖上层）
│   │   ├── jobs.py         # 任务管理（入队/领取/状态/后台轮询结果）
│   │   ├── queue.py        # 任务调度器（持久化队列 + 显存预算准入 + 多实例分发）
│   │   ├── vram.py         # 显存预算与峰值估算（决定能否并行）
│   │   └── schemas.py      # Pydantic 数据模型（请求/响应）
│   ├── comfyui/            # ComfyUI 对接（业务逻辑）
│   │   ├── client.py       # ComfyUI Server API 客户端（函数化封装）
│   │   ├── presets.py      # 模型预设加载器（model_configs/ 的 YAML）
│   │   └── workflows/      # 工作流图构建器（按功能拆分，注册式扩展）
│   │       ├── registry.py # 注册表：register / list / build
│   │       ├── txt2img.py  # 文生图（Checkpoint 模型）
│   │       ├── diffusion.py# 扩散模型文生图（Z-Image / Anima）
│   │       ├── quality.py  # 画质增强（超分放大重绘 + 脸部修复）
│   │       └── loras.py    # LoRA 叠加链
│   ├── prompting/          # AI 提示词 Agent（两阶段流水线，配置驱动）
│   ├── admin/              # 管理员后台：密码/会话/隐私数据/在线编辑提示词
│   ├── tests/              # 自测与浏览器点击测试
│   └── webui/              # 前端 WebUI（纯 HTML/CSS/JS）
│       ├── index.html
│       ├── css/style.css
│       └── js/
│           ├── app.js                # 核心逻辑（状态/选项/生成/历史）
│           ├── admin.js              # 管理后台逻辑（登录/编辑/改密）
│           └── features/
│               ├── quality.js        # 画质增强面板
│               └── loras.js          # LoRA 叠加面板
├── docs/                   # 文档（全功能文档 + 各目录 README 索引）
├── model_configs/          # 模型官方预设（傻瓜式参数，每个模型一个 YAML）
│   ├── z_image_turbo.yaml
│   ├── z_anime_distill_8step.yaml
│   ├── anima_aesthetic.yaml
│   ├── itns_comic_merge.yaml
│   └── README.md
├── scripts/                # 全部 .bat 脚本（启动/安装/管理员等）
│   ├── install_deps.bat
│   ├── start_comfyui.bat   # 无界面启动 ComfyUI 后端服务（端口 8188）
│   ├── start_webui.bat     # 启动我们的 WebUI（端口 8642）
│   └── README.md
├── config.yaml             # 运行配置（端口、ComfyUI 地址）
├── comfyui_extra_model_paths.yaml  # ComfyUI 模型路径映射（D 盘共享目录）
├── requirements.txt
```

## 各目录 README 索引

每个主要目录都有独立的 `README.md`，说明这一块是干什么的：

| 目录 | 说明 |
|---|---|
| [app/](app/README.md) | 应用代码总览（用户服务 + 管理后台两个入口） |
| [app/api/](app/api/README.md) | 用户服务 REST 接口层 |
| [app/core/](app/core/README.md) | 任务管理与数据模型 |
| [app/comfyui/](app/comfyui/README.md) | ComfyUI 对接（客户端 + 预设） |
| [app/comfyui/workflows/](app/comfyui/workflows/README.md) | 工作流图构建器 |
| [app/prompting/](app/prompting/README.md) | AI 提示词 Agent |
| [app/admin/](app/admin/README.md) | 管理员后台 |
| [app/webui/](app/webui/README.md) | 前端页面与脚本 |
| [app/tests/](app/tests/README.md) | 测试脚本 |
| [docs/](docs/README.md) | 文档索引（全功能文档见 [docs/功能文档.md](docs/功能文档.md)） |
| [model_configs/](model_configs/README.md) | 模型预设规范 |
| [scripts/](scripts/README.md) | `.bat` 脚本说明 |
| [data/](data/README.md) | 运行时数据目录 |

## 快速开始

1. 确保 Python 3.14 虚拟环境已创建（`.venv`）。
2. 双击 `scripts\install_deps.bat` 安装依赖。
3. 双击 `scripts\start_comfyui.bat` 无界面启动 ComfyUI 后端服务（脚本会等待 API 就绪）。
4. 双击 `scripts\start_webui.bat` 启动我们的 WebUI，浏览器访问 `http://localhost:8642`。

> 所有脚本统一放在 `scripts/` 目录，`.bat` 文件编码约定：纯 ASCII 或 GBK 中文，
> **禁止 UTF-8**（否则 cmd 解析会乱码/报错），具体见 `scripts\README.md`。

## 配置

编辑 `config.yaml`：

```yaml
server:
  host: 127.0.0.1
  port: 8642

comfyui:
  base_url: http://127.0.0.1:8188
  client_id: mpwe-webui
  timeout: 30
```

也可用环境变量覆盖：`MPWE_HOST`、`MPWE_PORT`、`COMFYUI_BASE_URL`。

## 模型预设（傻瓜式配置）

所有模型的生成参数（步数、CFG、采样器、调度器、分辨率，以及扩散模型配套的
文本编码器/VAE/CLIP 类型）都预设在 `model_configs/` 目录，来源为各模型官方文档：

| 模型 | 类别 | 预设参数 |
|---|---|---|
| Z-Image Turbo | 扩散模型 | 10 步 / CFG 1 / euler+simple / 1024x1536 / qwen_3_4b(lumina2)+ae |
| Z-Anime Distill 8-Step | 扩散模型 | 8 步 / CFG 1 / euler_ancestral+beta / 1024x1536 / qwen_3_4b(lumina2)+ae |
| Anima Aesthetic v1.1 | 扩散模型 | 30 步 / CFG 4 / euler+simple / 1024x1024 / qwen_3_06b(stable_diffusion)+qwen_image_vae |
| ItN's Comic Merge | Checkpoint | 20 步 / CFG 6 / euler+normal / 768x1152 |
| One Obsession V14 | Checkpoint | 33 步 / CFG 5 / euler_ancestral+normal / 832x1216 |
| Animagine XL 4.0 Opt Clear / INT8 | Checkpoint | 22 步 / CFG 5 / dpmpp_2m_sde+beta / 1024x1024 |
| ChenkinNoob XL V0.5 | Checkpoint | 28 步 / CFG 6 / euler_ancestral+normal / 1024x1024 |
| Illustrious XL v2.0 | Checkpoint | 32 步 / CFG 6 / euler_ancestral+karras / 1024x1024 |
| NovelAI Diffusion Anime V2 | Checkpoint | 28 步 / CFG 11 / euler+normal / 832x1216 / **CLIP skip=2（必须）** |

WebUI 选中模型后自动套用，用户无需手填。新增模型：把模型文件放进 ComfyUI 对应目录，
然后在 `model_configs/` 加一个 YAML（规范见 `model_configs/README.md`）。

提示词不在自动预设范围内；各模型的提示词写法指南（自然语言 / 标签 / 混用）也在对应 YAML 里。

## 画质增强（超分放大重绘 + 脸部修复）

每个 Checkpoint 模型在 `model_configs/*.yaml` 里都带 `quality:` 预设，WebUI 的「画质增强」面板
默认开启两项（可在界面关闭）：

1. **超分放大重绘（Hires fix）**：首遍生成 → 用 IllustrationJaNai V3 超分模型放大 2 倍 →
   VAE 编码回潜空间 → 低降噪强度二次采样 → 解码。默认 `2x_IllustrationJaNai_V3detail_SPAN_S_40k_fp16`
   （4060m 8G 显存实测可用，1664x2432 出图约 2 分钟）。
2. **脸部修复（FaceDetailer）**：用 YOLOv8 脸部检测（`face_yolov8m.pt`，备选 `face_yolov8n.pt`）
   裁出脸部区域单独重绘，修复崩脸/崩眼。检测模型放在
   `D:\Comfy-Desktop\ComfyUI-Shared\models\detection` 与
   `E:\AI\Confy UI\ComfyUI\ComfyUI\models\ultralytics\bbox`。

新增依赖（已安装）：

- ComfyUI 自定义节点 `ComfyUI-Impact-Pack` + `ComfyUI-Impact-Subpack`（gitcode 镜像安装，提供 FaceDetailer /
  UltralyticsDetectorProvider）；sam2 为可选依赖未安装，SAM 类节点不可用，不影响脸部修复。
- Python 包：`ultralytics`、`onnxruntime`（预留）、`opencv-python-headless`、`segment-anything` 等
  （安装在 ComfyUI 的 `.venv` 或 standalone-env，按 `start_comfyui.bat` 的 `PYTHONPATH` 约定加载）。

请求参数（`POST /api/generate` 新增字段）：`hires_fix`、`upscale_model`、`hires_denoise`、
`hires_steps`、`hires_cfg`、`face_detailer`、`face_detector`、`face_threshold`、`face_denoise`、
`face_steps`、`face_cfg`、`face_guide_size`、`face_max_size`。

## LoRA 叠加（WebUI 已支持）

WebUI 的「LoRA 叠加」面板会列出 `models/loras` 下全部文件，勾选即生效：
后端按顺序把每个 LoRA 串成 LoraLoader 链（model/clip 依次传递），权重可调，
`POST /api/generate` 的 `loras: [{file, weight}]` 字段传入。

每个模型的推荐 LoRA 与默认开关写在各 `model_configs/*.yaml` 的 `loras:` 列表里
（`default: true` 的项 WebUI 自动勾选）。类型约定：`quality` 整体画质、`style` 风格、
`hands` 手部修复、`hair` 发丝、`character` 角色、`nsfw` / `other` 按需求。

One Obsession V14 已配默认组合（全部有公开镜像、已验证可用）：

| LoRA | 类型 | 默认权重 | 用途 |
|---|---|---|---|
| Smooth_Booster_v5 | quality | 0.5 | 整体画质/细节增强 |
| markinson_solidline | style | 0.7 | 线条风格（触发词 `markinson_solidline`） |
| PerfectHandsIllustrious | hands | 0.7 | 修复六指/断指/畸形手掌 |
| Detailed_anime_style-ILv2 | hair | 0.6 | 蓬松有层次的发丝 |

备选：Hands XL v5.5（通用手部）、LECO_BadHands/GoodHands 组合（社区用法 ±1.0）、
NSFW 滑杆/细节/姿势（默认关闭，按需开启）。完整清单与下载来源见 `model_configs/README.md`。

## API 一览

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /mpwe/health | 后端与绘画GPU服务器连接状态 |
| GET | /mpwe/config | 运行配置 |
| GET | /mpwe/comfyui/status | 绘画GPU服务器系统信息 |
| GET | /mpwe/comfyui/models?category=checkpoints | 模型列表（checkpoints/loras/vae/upscale_models/...） |
| GET | /mpwe/comfyui/samplers | 采样器列表 |
| GET | /mpwe/comfyui/schedulers | 调度器列表 |
| GET | /mpwe/comfyui/workflows | 已注册的工作流功能（txt2img / z_image_txt2img / anima_txt2img / quality_pass） |
| GET | /mpwe/comfyui/clip_types | CLIPLoader 支持的文本编码器类型 |
| GET | /mpwe/comfyui/diffusion_presets | 扩散模型官方预设（VAE / 文本编码器 / CLIP 类型 / 推荐参数） |
| GET | /mpwe/comfyui/checkpoint_presets | Checkpoint 模型官方预设 |
| POST | /mpwe/generate | 提交生成任务 |
| GET | /mpwe/queue | GPU 队列状态（运行中/排队数/显存预算/实例列表） |
| GET | /mpwe/jobs | 历史任务 |
| GET | /mpwe/jobs/{id} | 任务状态与结果 |
| GET | /mpwe/jobs/{id}/images/{index} | 任务输出图片 |
| POST | /mpwe/prompt/translate | 提交 AI 提示词翻译任务（大白话 → 模型特调提示词） |
| GET | /mpwe/prompt/jobs/{id} | 查询提示词翻译任务状态与结果 |
| GET | /mpwe/prompt/agents | 查询已配置专属 Agent 的模型清单（一个模型对应一个 Agent） |

> 前端统一走 `/mpwe` 前缀（绕开广告拦截器常见的 `/api/` 过滤规则）；
> `/api` 前缀保留兼容旧调用。静态资源带版本号（`?v=...`）防 Cloudflare 缓存旧版。

交互式 API 文档：启动后访问 `http://localhost:8642/docs`。

## 排队与并行（多用户）

任务提交后先进入业务层持久化队列（SQLite，`status=queued`），由后台调度器按
**优先级 + 创建时间** 领取，再按显存预算放行到空闲的 ComfyUI 实例：

- **排队**：每个用户最多同时运行 `queue.max_concurrent_per_user` 个任务
  （默认 2，防单用户占满队列）；前端实时显示「队列第 N 位」与 GPU 队列状态。
- **并行准入**：同时运行的任务峰值显存之和 ≤ 总显存 − 余量才允许并行。
  峰值数据在 `data/vram_estimates.json`（实测 + 同族代理），按 模型 × 分辨率 取档，
  未测模型按文件大小保守估算；因此 **SD1.5 小分辨率可自动 2 路并行，
  SDXL / Z-Image（含 Qwen 文本编码器）自动单路**。
- **多实例**：`config.yaml` 的 `comfyui.workers` 配并行实例列表
  （与 `scripts/start_comfyui.bat` 的 `MPWE_COMFYUI_WORKERS` 对应），
  每个实例同时只跑 1 个任务（ComfyUI 单进程单卡限制），调度器轮询分配，
  实例提交失败会临时拉黑并换实例重试。
- **取消**：排队任务直接撤单；运行中任务只从所在实例的队列删除，
  不再全局 interrupt 误伤其他任务。
- **重启恢复**：服务启动时残留 `running` 任务标记失败并把对应 prompt 移出
  ComfyUI 队列，`queued` 任务自动重新派发。

新增模型想校准并行档位，运行 `scripts\measure_vram.py`
（逐模型逐分辨率实测峰值，自动合并进 `data/vram_estimates.json`）。

## AI 提示词 Agent

WebUI 参数面板顶部新增「AI 提示词 Agent」：输入大白话，点击「⚡ AI 生成提示词」，
Agent 自动生成所选模型专用的完整正向 + 负向提示词并回填输入框，可手改后生图。

WebUI 交互细节：生成任务（含画质增强）有实时进度条（从 ComfyUI websocket 拉取）；
生成结果居中等比展示，点击图片可打开灯箱放大查看（Esc / 点击背景关闭）。

流程为两阶段（设计来自 MPWE 早期版本经验）：

1. **扩写 Agent（共用）**：大白话 → 600-700 字中文「画面需求书」（主次/构图/主体/道具/环境/光线/画风）；
2. **翻译 Agent（按模型特调）**：需求书 → 全英文 POSITIVE/NEGATIVE 提示词。

所有 Agent 的系统提示词集中在一个可编辑文件：
`app/prompting/agent_prompts.yaml`（Agent 名字 + 提示词 + 校验参数）。
修改后保存即可生效（下次请求自动重载），无需重启。

- `expand_agent`：共用扩写 Agent，所有模型一致；
- `models.<model_file>.translate_agent`：每模型专属翻译 Agent，
  新增模型时复制一段并改 `model_file` 与 `prompt` 即可，不改代码。

**严格一对一（不混用）**：翻译 Agent 只在 `models:` 里配置过的模型上可用。
不存在通用回退，因此特调提示词永远不会被混用到别的模型上。

当前已适配全部 10 个预设模型（与 `model_configs/*.yaml` 一一对应）：

| 模型 | Agent（提示词风格） |
|---|---|
| One Obsession V14 | Danbooru 标签 + 官方质量前缀 + 风格开关 |
| Z-Image Turbo | 纯自然语言长句；官方明确无负面提示词（只输出 POSITIVE） |
| Z-Anime Distill 8-Step | 纯自然语言长句；8 步蒸馏下负面词效果有限（官方） |
| Anima Aesthetic v1.1 | 标签 + 自然语言混用，官方 `score_7, safe` 前缀 |
| Animagine XL 4.0 Opt Clear / INT8 | 官方质量词 `masterpiece, high score, great score, absurdres` + 官方负面模板 |
| ChenkinNoob XL V0.5 | 官方正面 `masterpiece, best quality, newest, high resolution, aesthetic, excellent, year 2026` |
| Illustrious XL v2.0 | Danbooru 标签；`masterpiece, best quality, amazing quality` 前置 + `very aesthetic, newest` |
| ItN's Comic Merge | Danbooru 标签（SD1.5，77 token 预算；背景/服装从简） |
| NovelAI Diffusion Anime V2 | 纯 Danbooru 标签（CLIP skip 2）；V2 已移除 `masterpiece`，用 `best quality, amazing quality` |

为某个模型新增/调整专属 Agent 只需改
`app/prompting/agent_prompts.yaml`（key 用模型文件名，提示词全英文特调），
保存即生效：用户端按钮自动点亮、管理后台自动出现该 Agent，前后端无需改代码。
全部特调提示词均依据官方文档/官方页面与社区指南重写，来源要点写在每个 `prompt:` 内，
完整的扩展开发文档见 `app/prompting/README.md`。

LLM 配置：`config.yaml` 的 `llm:` 段 + 项目根目录 `.env`（参考 `.env.example`，
填入 `OPENAI_API_KEY`）。默认走 DeepSeek V4 Flash（API 模型名 `deepseek-v4-flash`，
旧的 `deepseek-chat` 已于 2026-07-24 停用；OpenAI 兼容接口）。

自测：`.venv\Scripts\python.exe app\tests\selftest_prompt_agent.py`
（加 `--live` 用真实 LLM 冒烟，需已配置 Key）。

## 扩展方式

### 新增一个生成功能（工作流）

在 `app/comfyui/workflows/` 下新建功能模块并注册：

```python
@register("img2img")   # 功能名
def build_img2img(**params) -> dict:
    """图生图工作流。"""
    return { ... }      # ComfyUI API 格式的工作流图
```

注册后 `GET /api/comfyui/workflows` 会自动列出，前端接入时把 `workflow` 字段传给它即可。

### 新增 ComfyUI 接口调用

在 `app/comfyui/client.py` 的 `ComfyUIClient` 中新增方法，例如：

```python
def upload_image(self, image_bytes: bytes, filename: str, subfolder: str = "") -> dict:
    return self._post("/upload/image", files=...).json()
```

### 新增后端接口

在 `app/api/routes.py` 的 `create_router()` 中新增路由函数。

## 技术栈

- 后端：Python 3.14 + FastAPI + uvicorn + requests
- 前端：原生 HTML / CSS / JavaScript（无构建步骤）
- 绘图引擎：本地 ComfyUI（Server API）
