# 隐私数据目录

本目录存放**本机私有数据**，已被 `.gitignore` 排除，**不要提交到 GitHub**：

| 文件 | 说明 |
|---|---|
| `secrets.json` | 管理员密码哈希 + 固定盐（首次启动生成，提交时不要带上） |
| `accounts.db` | 用户账号、邀请码、会话（用户名明文、密码盐/哈希） |

本地首次部署时运行：

```bat
scripts\启动管理员后台WebUI.bat
```

会在此目录自动生成 `secrets.json` 并设置管理员密码。
